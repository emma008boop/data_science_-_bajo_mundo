os deja interactuar con el sistema operativo.

os.environ -> es un diccionario que contiene las variables de entorno del sistema. es muy útil para leer credenciales o llaves API secretas sin ponerlas directamente en el código 

os.system(comando) -> Ejecuta un comando directamente en la terminal (Como os.system("ping google.com))

![[Pasted image 20260629132722.png]]

